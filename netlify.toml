[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for all client-side routes so deep
# links (e.g. /insights/news) don't 404 on refresh/direct load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200